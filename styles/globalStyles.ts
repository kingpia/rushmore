import { StyleSheet } from "react-native";

export const globalStyles = StyleSheet.create({
  loadingContainer: {
    flexDirection: "row",
    alignItems: "center",
  },
  activityIndicator: {
    marginRight: 10,
  },
  loadingText: {
    color: "#ffffff",
  },
});
