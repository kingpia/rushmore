import { SafeAreaView } from "react-native";
import { Text } from "react-native-paper";

export const RushmoreSettingsScreen = () => {
  return (
    <SafeAreaView>
      <Text variant="displayLarge">Rushmore Settings Screen</Text>
    </SafeAreaView>
  );
};
