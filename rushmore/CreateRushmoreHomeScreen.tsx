import { SafeAreaView, StyleSheet } from "react-native";
import { Text } from "react-native-paper";

export const CreateRushmoreHomeScreen = () => {
  return (
    <SafeAreaView>
      <Text variant="displayLarge">Create Home</Text>
    </SafeAreaView>
  );
};
