import { SafeAreaView } from "react-native";
import { Text } from "react-native-paper";

export const RushmoreRankingScreen = () => {
  return (
    <SafeAreaView>
      <Text variant="displayLarge">Rushmore Ranking Screen</Text>
    </SafeAreaView>
  );
};
