import { SafeAreaView } from "react-native";
import { Text } from "react-native-paper";

export const DeleteAccountSettingsScreen = () => {
  return (
    <SafeAreaView>
      <Text variant="displayLarge">DeleteAccountSettingsScreen</Text>
    </SafeAreaView>
  );
};
