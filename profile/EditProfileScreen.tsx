import { SafeAreaView } from "react-native";
import { Text } from "react-native-paper";

export const EditProfileScreen = () => {
  return (
    <SafeAreaView>
      <Text variant="displayLarge">Edit Profile Screen</Text>
    </SafeAreaView>
  );
};
